# Product Requirements Document: The Digital Student Experience

## Overview

Build a short web-based survey targeted at undergraduate students that collects information about their digital habits and campus presence. The app has two pages: a survey form and a results page showing aggregated responses. Responses are stored in a Supabase PostgreSQL database.

---

## Tech Stack

- **Framework:** Vite + React
- **Database:** Supabase (PostgreSQL)
- **Hosting:** Azure Static Web Apps
- **Charting:** Recharts

---

## Survey Questions

The survey must include the following four questions, using the specified input types:

| #   | Question                                                                        | Input Type                          |
| --- | ------------------------------------------------------------------------------- | ----------------------------------- |
| 1   | What is your major?                                                             | Text input box                      |
| 2   | What year are you in college?                                                   | Radio buttons                       |
| 3   | What is your favorite study spot on campus?                                     | Short text                          |
| 4   | Which building do you have the most classes in?                                 | Dropdown                            |
| 5   | What is your primary device for taking notes?                                   | Radio Buttons                       |
| 6   | Which software/apps do you use daily for school?                                | Checkboxes + conditional text input |
| 7   | If you could add one digital tool to your major's curriculum, what would it be? | Text Area                           |

### Question Details

**Q1 — Major**
- Free-text input
- Placeholder: "e.g. Business Analytics"
- Required field

**Q2 — Year in College**

- Radio button group with the following options (select one):
  - 1st Year
  - 2nd Year
  - 3rd Year
  - 4th Year
  - 5th Year or More
- Required field

**Q3 - Favorite Study Spot**

- Free-text input
- Placeholder: "e.g. BizHub"
- Required field

**Q4 — Primary Building (Dropdown)**

- Options: 
  - Pappajohn Business Building
  - Pomerantz Center
  - Chemistry Building
  - Macbride Hall
  - Shaeffer Hall
  - Other
- Required field

**Q5 - Primary Note-Taking Device**

- Radio button group with the following options (select one):
  - Laptop (Mac/PC)
  - Tablet (iPad/Surface)
  - Pen and Paper
  - Phone
- Required field

**Q6 — Software/Apps (Checkboxes)**

- Checkboxes with the following options: 
  - Canvas / ICON, 
  - Microsoft Word / Microsoft Excel
  - Microsoft Teams / Zoom
  - Notion / GoodNotes
  - ChatGPT / AI Tools
  - Other
- If "Other" is checked, display a text input box asking the user to describe their other software
- At least one checkbox must be selected (required)
- The "Other" text box is required if "Other" is checked

**Q7 — Cirriculm Suggestion**

- Large text area
- Placeholder: "What digital tool should be added to your major?"
- Required field


---

## Form Behavior

- All fields are required; display inline validation error messages if the user submits without completing a field
- The "Other" building and software/apps text box appears dynamically only when "Other" is choosen
- On successful submission, display a thank-you confirmation screen showing a summary of the user's answers
- The thank-you screen includes two buttons:
  - "Submit Another Response" — resets the form
  - "View Results" — navigates to the results page
- The submit button shows "Submitting..." while the request is in flight
- If the database insert fails, display an error message and keep the form intact

---

## Results Page

A separate `/results` route displays aggregated, anonymized data from all submissions. Individual responses are never shown — only group-level summaries.

### Charts / Visualizations

1. **Total Responses** — a large number prominently displayed at the top
2. **Year in College** — vertical bar chart, one bar per year option, ordered chronologically
3. **App Usage Popularity** — Horizontal bar chart sorted by count descending. For "Other" entries, normalize casing (e.g., "Slack" and "slack" are the same).
4. **Major Distribution** — pie chart or horizontal bar chart showing the top majors represented

Use a charting library (e.g. Recharts) for all visualizations.

### Navigation

- Home page has a short welcome message with a "Take the Survey" button linking to the survey form and a "View Results" button linking to the results page
- The results page has a "Home" button linking back to the home page
- The survey form has a "View Results" button in the header linking to the results page

---

## Database

I am using my own Supabase account. Run the following SQL in the Supabase SQL Editor to create the table and configure access.

### Create Table

```sql
create table digital_experience_results (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz default now(),
  major text not null,
  year_in_college text not null,
  study_spot text not null,
  primary_building text not null,
  note_device text not null,
  apps text[] not null,
  other_app text,
  curriculum_suggestion text
);
```

'apps' is stored as a PostgreSQL text array. 
'other_app' and 'curriculum_suggestion' are nullable — only populated when "Other" is selected or when the user provides a suggestion.

### Row-Level Security

Supabase enables RLS by default and will block all inserts and selects unless policies are configured. Because this is a public survey with no authentication, run the following:

```sql
alter table digital_experience_results enable row level security;

create policy "Allow public insert"
  on digital_experience_results for insert
  to anon
  with check (true);

create policy "Allow public select"
  on digital_experience_results for select
  to anon
  using (true);
```

### Environment Variables

The app connects to Supabase using two environment variables:

| Variable                 | Description                                                 |
| ------------------------ | ----------------------------------------------------------- |
| `VITE_SUPABASE_URL`      | Your Supabase project URL (e.g. `https://xxxx.supabase.co`) |
| `VITE_SUPABASE_ANON_KEY` | Your Supabase anon/public key                               |

Both values are found in the Supabase dashboard under **Project Settings → API**.

- For local development, create a `.env` file in the project root with these values. Add `.env` to `.gitignore`.
- For production, add both as GitHub Actions secrets. The CI/CD workflow injects them at build time.

---

## Design & Quality Standards

### Appearance

- Simple, modern, minimalist design throughout
- Cream or light neutral background with dark body text
- `#FFB6C1` is the primary accent color — use it for buttons, links, active/focus states, borders, and chart colors
- Do not use `#FFB6C1` for body text; it does not meet WCAG AA contrast requirements at small sizes. Use a dark neutral (e.g. `#1a1a1a`) for body text
- For any text rendered in the accent color (e.g. headings or labels), verify contrast passes WCAG AA before using it

### Responsive Layout

The app must be usable on mobile, tablet, and desktop screen sizes. Use a single-column layout on small screens. Do not assume desktop-only use — the survey will likely be completed on phones.

### HTML5 Validation

All pages must validate as HTML5 with no errors using the [W3C Markup Validation Service](https://validator.w3.org/).

### Accessibility — WCAG 2.1 Level AA

All pages must pass WCAG 2.1 Level AA. Key requirements:

- All form inputs must have a programmatically associated `<label>` element
- Inline validation error messages must be programmatically associated with their fields using `aria-describedby`
- The "Other" building and software/apps text input must be keyboard accessible when it appears dynamically — focus should move to it automatically when "Other" is choosen
- All images and icons must have appropriate `alt` text
- Color alone must not be used to convey meaning (e.g. error states must include text, not just a red border)
- All interactive elements must be reachable and operable via keyboard
- Use sufficient color contrast for all text — minimum 4.5:1 for normal text, 3:1 for large text

---

## Footer

Every page displays a footer with the following text:

> Survey by Mia Maiers, BAIS:3300 - Spring 2026.

---

## Deployment

This app is deployed as an **Azure Static Web App** using Vite + React.

Because the app uses client-side routing (`/` and `/results`), a `staticwebapp.config.json` file must be included in the `public/` directory so Azure serves `index.html` for all routes instead of returning 404s on direct navigation or page refresh.

```json
{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*"]
  }
}
```

This file is automatically copied to `dist/` during the Vite build.
